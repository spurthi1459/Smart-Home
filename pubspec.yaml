name: my_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6          # Helps app talk to Alexa
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.5.2
  email_validator: ^2.1.17   # Added email_validator package

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
